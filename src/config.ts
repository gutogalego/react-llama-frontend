// src/config.ts
const config = {
    API_BASE_URL: 'http://127.0.0.1:8000',
    ENDPOINTS: {
      TEXT_COMPLETION: '/autocomplete',
      SUMMARIZE: '/summarize'
    },
  };
  
  export default config;
  